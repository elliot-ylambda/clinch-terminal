#!/usr/bin/env bash
# Compatibility shim for older hooks and user scripts. New integrations invoke
# clinch-agent-resume directly.
set -euo pipefail
BIN="$(cd "$(dirname "$0")" && pwd)"
exec "$BIN/clinch-agent-resume" "$@"
